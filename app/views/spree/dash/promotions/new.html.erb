<% content_for :page_title do %>
  <%= link_to Spree.t(:promotions), dash_promotions_url %> /
  <%= Spree.t(:new_promotion) %>
<% end %>
<div class="card">
  <div class="card-body">
    <%= form_with model: :promotion, url: collection_url do |f| %>
      <%= render partial: 'form', locals: { f: f } %>
      <%= render partial: 'spree/dash/shared/new_resource_links' %>
    <% end %>
  </div>
</div>
